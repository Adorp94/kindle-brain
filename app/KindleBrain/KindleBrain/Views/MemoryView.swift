import SwiftUI

// Memory feature removed for MVP. Kept as placeholder.
struct MemoryView: View {
    var body: some View {
        Text("Memory feature coming soon")
            .foregroundStyle(.secondary)
    }
}
